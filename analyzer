#!/usr/bin/env python3
import sys
import os

# Add the project root to sys.path so tools.analyzer can be imported
project_root = os.path.abspath(os.path.dirname(__file__))
if project_root not in sys.path:
    sys.path.insert(0, project_root)

from tools.analyzer.cli import main

if __name__ == "__main__":
    main()
